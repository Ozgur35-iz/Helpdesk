# Optional. Error reporting DSN for @sentry/react. Leave blank to disable.
# Read at build time (Vite inlines it into the bundle), so it must be set
# wherever `bun run build` runs — e.g. as a build-time variable on the
# Railway web service, not just at runtime.
VITE_SENTRY_DSN=""
