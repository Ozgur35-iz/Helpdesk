{
  "name": "project-scope",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "dev:server": "bun --filter server dev",
    "dev:client": "bun --filter client dev"
  }
}
